---
name: Bug report
about: The CLI produces wrong output, crashes, or hangs
title: '[bug] '
labels: bug
assignees: ''
---

### What happened?

<!-- One paragraph: what command you ran, what you expected, what you got. -->

### How to reproduce

1. Sitemap source (URL or path to a local fixture):
2. Exact CLI command you ran:

```sh
geosuite-llms-txt --sitemap <…>
```

3. Output you got (paste full stdout/stderr or a relevant excerpt):
4. Output you expected:

If the sitemap is private, a minimal anonymized fixture is the fastest path
to a fix — see `test/fixtures/` for examples.

### Environment

- `@geosuite/llms-txt-generator` version (`npm list @geosuite/llms-txt-generator` or git SHA):
- Node version (`node --version`):
- OS:

### Anything else?

<!-- Link to the live sitemap if public, screenshots of the rendered llms.txt,
or behaviour you observed in a third-party llms.txt parser. -->
